<launch>

  <arg name="map_file" default="$(find provide_map)/src/my_map.yaml"/>

  <!-- Run the map server -->
  <node name="map_server" pkg="map_server" type="map_server" args="$(arg map_file)" />

</launch>